import React from 'react';
import { Text, View } from 'react-native';

const Account = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text style={{fontSize: 20, fontWeight: '600'}}>Account screen</Text>
        </View>
    );
};

export default Account;