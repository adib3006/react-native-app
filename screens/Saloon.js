import React from 'react';
import { Text, View } from 'react-native';

const Saloon = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text style={{fontSize: 20, fontWeight: '600'}}>Saloon screen</Text>
        </View>
    );
};

export default Saloon;